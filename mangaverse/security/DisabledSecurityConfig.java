package com.mangaverse.security;

// Duplicate security config disabled. The application uses `com.mangaverse.config.SecurityConfig`.
// Keeping this file to avoid breaking imports, but it is intentionally not a @Configuration so
// Spring will not treat it as a source of beans.
public class DisabledSecurityConfig {
    // Disabled duplicate configuration - handled by com.mangaverse.config.SecurityConfig
}